export const metadata = {
  title: "Private Driver Tracking",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function DriverLayout({ children }) {
  return children;
}
